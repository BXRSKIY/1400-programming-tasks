# 12.97. Дана строка. Определить, образует ли подстрока с символа m по символ n число 666.
# Нумерация с 1. Ввод: строка, затем m n.
s = input().rstrip("\n")
m, n = map(int, input().split())
sub = s[m-1:n]
print("да" if sub == "666" else "нет")
