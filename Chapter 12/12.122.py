# 12.122. Дана строка. Удалить из нее все пробелы.
# Удаление по правилам 12.116–12.124: сдвиг влево + '_' в конец (по числу удалённых символов).
s = input().rstrip("\n")
removed = s.count(" ")
t = "".join(ch for ch in s if ch != " ") + "_" * removed
print(t)
