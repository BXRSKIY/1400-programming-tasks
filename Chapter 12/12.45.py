# 12.45. Сформировать строку из заданного количества одинаковых символов.
# Ввод: n, затем символ (или строка, берется первый символ).
n = int(input())
sym = input().rstrip("\n")
ch = sym[0] if sym else ""
print(ch * n)
